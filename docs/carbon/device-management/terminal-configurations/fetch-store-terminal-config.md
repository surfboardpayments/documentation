# Fetch Store Terminal Config

Retrieves the terminal configuration settings for a specific store. It returns details about enabled features, supported terminal features, and other store-level terminal configurations.