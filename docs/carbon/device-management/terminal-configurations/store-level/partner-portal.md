1. Set store-level configuration can be done by clicking **Merchants**→ **Stores** → select the desired store.
2. In the top-right corner, click **Set Store Terminal Config**.
3. Provide the needed details for setting in the store-level configs and click **Save Changes**.
