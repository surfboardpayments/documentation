# Fetch Merchant Terminal Config

Retrieves the terminal configuration settings associated with a specific merchant. It returns details such as active features, supported payment methods, and other terminal-related configurations to help you manage or validate how the merchant’s terminals are set up.