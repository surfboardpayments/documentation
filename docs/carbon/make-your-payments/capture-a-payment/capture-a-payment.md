# 2. Capture a Payment

This call is initiated when an order is created with delay capture alone and also with authMode. It completes the order by capturing the full payment during checkout using the paymentId from the initiate payment step.
