<!-- 
  Generated by: Surfboard Payments LLM Context Generator
  Version: 1.0.0
  Generated at: 2025-12-20T10:26:55.178Z
  
  This file is auto-generated. Do not edit manually.
-->

# API Integration Context: Fetch All Store Terminals

> This document provides comprehensive context for integrating with our API.
> Use this with an LLM to get help implementing the Fetch All Store Terminals flow.

## Overview

**Category:** Store Capabilities
**Scenario:** Fetch All Store Terminals
**Description:** Fetch all store terminals

Learn how to use other functionality of store

---

## Steps Overview

This scenario involves **1** step:

1. **Fetch All Store Terminals** - Fetches all terminals registered under a store.

---

## Step 1: Fetch All Store Terminals

### Endpoint

```
GET partners/{{partnerId}}/merchants/{{merchantId}}/stores/{{storeId}}/terminals
```

### Description

Fetches all terminals registered under a store.

### Specifications

### Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `terminalType` | string | No | Optional query param to filter terminals by terminal type. |

### Request Payload

```json
{}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request. 'SUCCESS' \| 'ERROR'. |
| `data` | array | Response data. |
| `data.terminalId` | String | Store ID of the terminal. |
| `data.terminalType` | string | Describes the type of the terminal. Possible values: `surfpad`, `surftouch`, `surfprint`, `checkoutPro`, `checkoutX`, `PaymentPage`, `SelfHostedPage`, `MerchantInitiated`, `printer`, `surftester` |
| `data.softwareVersion` | String | Software version of the terminal. |
| `data.osType` | string | Describes the type of operating system running on the terminals. Possible values: `android`, `ios`, `sb_firmware` |
| `data.osVersion` | String | Operating System(OS) version of the terminal. |
| `data.deviceVendor` | String | Device vendor of the terminal. |
| `data.deviceModel` | String | Device model of the terminal. |
| `data.serialNo` | String | Serial number of the terminal. |
| `data.storeId` | String | The Store ID of the store under which the terminal is registered. |
| `data.linkedTerminalId` | String | The Terminal ID of another terminal to which the current terminal is linked. |
| `data.terminalStatus` | String | Status of the terminal. The terminal can be in any one of these states: 'REGISTERED' \| 'ACTIVE' \| 'IN_ACTIVE' \| 'DE_REGISTERED’. |
| `data.terminalPaymentMethods` | String | Payment methods supported by the terminal. |
| `data.startDate` | String | Timestamp at which terminal was registered. |
| `data.terminalName` | string | Name of the terminal used as a reference for users. |
| `message` | string | Message describing the status of the request. |

### Guide

# Fetch All Store Terminals

Using this can fetch a list of all terminals associated with a specific store under a particular merchant.

---

## Environment Variables

The following template variables are used in this scenario:

| Variable | Description |
|----------|-------------|
| `{{partnerId}}` | Your partner ID |
| `{{merchantId}}` | The merchant ID |
| `{{storeId}}` | The store ID |

## Quick Reference

| Step | Method | Endpoint | Purpose |
|------|--------|----------|---------|
| 1 | GET | `partners/{{partnerId}}/merchants/{{merchantId}}/stores/{{storeId}}/terminals` | Fetches all terminals registered under a store. |

