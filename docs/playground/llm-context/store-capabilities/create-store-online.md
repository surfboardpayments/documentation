<!-- 
  Generated by: Surfboard Payments LLM Context Generator
  Version: 1.0.0
  Generated at: 2025-12-20T10:26:55.171Z
  
  This file is auto-generated. Do not edit manually.
-->

# API Integration Context: Create Store with Online Functions

> This document provides comprehensive context for integrating with our API.
> Use this with an LLM to get help implementing the Create Store with Online Functions flow.

## Overview

**Category:** Store Capabilities
**Scenario:** Create Store with Online Functions
**Description:** Create store with online functions

Learn how to use other functionality of store

---

## Steps Overview

This scenario involves **2** steps:

1. **Create Store** - Create a new online store or update an existing physical store to an online store
2. **Fetch Store Details** - Fetches the details of a store under the partner for a specific merchant.

---

## Step 1: Create Store

### Endpoint

```
POST partners/{{partnerId}}/merchants/{{merchantId}}/stores
```

### Description

Create a new online store or update an existing physical store to an online store

### Specifications

### Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `storeName` | string | Yes | Name of the store. |
| `email` | string | No | Email address of the store. Email address in mandatory to support online payments. |
| `phoneNumber` | object | Yes | Contact number of the store in code and number format. |
| `phoneNumber.code` | number | Yes | International dialing code identifying the country or region. |
| `phoneNumber.number` | string | Yes | A string of numbers ranging from 0-9 with a length of 5-15 characters. |
| `address` | string | Yes | Address line 1 of the store. |
| `careOf` | string | No | Name of the addressee that is accepting the correspondence for the intended recipient. |
| `city` | string | Yes | Name of the city where the store is located. |
| `zipCode` | string | Yes | ZIP code of the store. |
| `country` | string | Yes | Two-letter ISO country code in uppercase, representing the location of the store. |
| `acquirerMID` | string | No | Acquirer Merchant ID is a mandatory parameter for PF partners who want to support a store based acquiring model. |
| `onlineInfo` | object | No | In order to start accepting online payments you need to create an online store which requires some additional properties such as the following. |
| `onlineInfo.merchantWebshopURL` | string | Yes | This is the web-shop URL of the merchant. |
| `onlineInfo.paymentPageHostURL` | string | No | This is the URL of the payment page. This is required for integrating online payments using SDK mode. |
| `onlineInfo.termsAndConditionsURL` | string | Yes | This is the URL of the T&C of the merchant’s web-shop, it has to contain the refund policy. |
| `onlineInfo.privacyPolicyURL` | string | Yes | This is the URL of the privacy policy of the merchant. |

### Request Payload

```json
{
  "storeName": "Trial Store",
  "email": "TS@gmail.com",
  "phoneNumber": {
    "code": 91,
    "number": "3214576980"
  },
  "address": "10,",
  "city": "Stockholm",
  "zipCode": "103 16",
  "country": "SE",
  "onlineInfo": {
    "merchantWebshopURL": "https://testmerchantportal.com/home",
    "paymentPageHostURL": "https://testmerchantportal.com/payment",
    "termsAndConditionsURL": "https://testmerchantportal.com/terms",
    "privacyPolicyURL": "https://testmerchantportal.com/privacy"
  }
}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request. |
| `data` | object | Response data. |
| `data.storeId` | string | Store ID of the created store. |
| `data.merchantId` | string |  The Merchant ID of the merchant under which the store is created. |
| `data.name` | string | Name of the store. |
| `data.address` | object | Address of the store. |
| `data.address.careOf` | string | Name of the addressee that is accepting the correspondence for the intended recipient. |
| `data.address.addressLine1` | string | First line of address. |
| `data.address.addressLine2` | string | Second line of address. |
| `data.address.addressLine3` | string | Third line of address. |
| `data.address.city` | string | Name of the City where the store is located. |
| `data.address.countryCode` | string | Two-letter ISO country code in uppercase, representing the location of the store. |
| `data.address.postalCode` | string | Postal Code of the place where the store is located. |
| `data.phone` | string | Contact number of the store. |
| `data.email` | string | Email address of the store. |
| `data.merchantURLDomainVerficationKey` | string | This is a key to set and verify that the merchant owns the domain of the URL. |
| `data.paymentPageURLDomainVerficationKey` | string | This is a key to set and verify the payment page domain URL. |
| `message` | string | A message that describes the status of the request |

### Guide

# Create Store with Online Functions

Online stores in Surfboard function similarly to physical stores but require additional properties to facilitate online payments. You can either create a new online store or update an existing physical store to an online store 

#### Using API

To create a new online store or update an existing physical store, you will use the **Create Store API** with additional parameters for online functionalities.

1. **Create or Update Store**: Send a `POST` request to the **`Create New Store API`** or **`Update Store API`** with the necessary parameters for online functionality.
2. **Domain Verification**: Upon creation, you will receive **domain validation keys** for both the `paymentpageURL` (if provided) and the `merchantWebshopURL`.
    - You **must** set this key as a **TXT record** on your domain server.
    - Use the **`Verify Domain API`** to initiate verification. Surfboard automatically performs checks every 6 hours.
3. **Receive Store ID**: A unique **`Store ID`** is returned on successful creation/update, which is essential for registering online terminals and subsequent store operations.
4. **Complete Onboarding**: The store enters an internal approval process. Once approved, you are authorized to register online terminals under that store.

#### Using Partner Portal

1. Log in to the **Partner Portal**, go to the **Merchants** tab, and select the desired merchant.
2. In the **Stores** section, click **Edit** next to the store, then select **Edit Store Details**.
3. Update the necessary fields to mark it as an online store and click **Update Store**.

---

## Step 2: Fetch Store Details

### Endpoint

```
GET partners/{{partnerId}}/merchants/{{merchantId}}/stores/{{storeId}}
```

### Description

Fetches the details of a store under the partner for a specific merchant.

### Specifications

### Request Payload

```json
{}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request. |
| `data` | object | Response data. |
| `data.storeId` | string | Store ID of the store. |
| `data.merchantId` | string | The Merchant ID of the merchant under which the store is created. |
| `data.name` | string | Name of the store. |
| `data.status` | string | Status of the store. Possible values: `ACTIVE`, `DEACTIVATED`, `BLOCKED`, `INACTIVE` |
| `data.onlineOnboardingStatus` | string | Onboarding status of the online store. Possible values: `APPROVED`, `INITIATED`, `FAILED` |
| `data.address` | object | Address of the store. |
| `data.address.careOf` | string | Name of the addressee that is accepting the correspondence for the intended recipient. |
| `data.address.addressLine1` | string | The first line of the store's address. |
| `data.address.addressLine2` | string | The second line of the store's address. |
| `data.address.addressLine3` | string | The third line of the store's address. |
| `data.address.city` | string | Name of the City where the store is located. |
| `data.address.countryCode` | string | Two-letter ISO country code in uppercase, representing the location of the store. |
| `data.address.postalCode` | string | Postal Code of the place where the store is located. |
| `data.phone` | string | Contact number of the store.  |
| `data.email` | string | Email address of the store.  |
| `data.onlineInfo` | object | In order to start accepting online payments you need to create an online store which requires some additional properties such as the following. |
| `data.onlineInfo.merchantWebshopURL` | string | This is the web-shop URL of the merchant. |
| `data.onlineInfo.paymentPageHostURL` | string | This is the URL of the payment page. |
| `data.onlineInfo.termsAndConditionsURL` | string | This is the URL of the T&C of the merchant’s web-shop, it has to contain the refund policy. |
| `data.onlineInfo.privacyPolicyURL` | string | This is the URL of the privacy policy of the merchant. |
| `message` | string | Message describing the status of the request. |

### Guide

# Fetch Store Details

You can retrieve information regarding a particular store created under a merchant.

#### Using API

Retrieves information for a specific store. Use this API to get information regarding a particular store created under a merchant.

#### Using Partner Portal

1. Login **Partner Portal**, go to **Merchants** → select the desired merchant.
2. Go to the **Stores** section to view their details.


---

## Environment Variables

The following template variables are used in this scenario:

| Variable | Description |
|----------|-------------|
| `{{partnerId}}` | Your partner ID |
| `{{merchantId}}` | The merchant ID |
| `{{storeId}}` | The store ID |

## Quick Reference

| Step | Method | Endpoint | Purpose |
|------|--------|----------|---------|
| 1 | POST | `partners/{{partnerId}}/merchants/{{merchantId}}/stores` | Create a new online store or update an existing ph... |
| 2 | GET | `partners/{{partnerId}}/merchants/{{merchantId}}/stores/{{storeId}}` | Fetches the details of a store under the partner f... |

