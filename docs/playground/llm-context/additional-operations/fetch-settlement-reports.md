<!-- 
  Generated by: Surfboard Payments LLM Context Generator
  Version: 1.0.0
  Generated at: 2026-01-06T08:11:20.845Z
  
  This file is auto-generated. Do not edit manually.
-->

# API Integration Context: Fetch Settlement Reports

> This document provides comprehensive context for integrating with our API.
> Use this with an LLM to get help implementing the Fetch Settlement Reports flow.

## Overview

**Category:** Additional Operations
**Scenario:** Fetch Settlement Reports
**Description:** Fetch Settlement Reports

Learn how to manage other operations based on merchant and store

---

## Steps Overview

This scenario involves **1** step:

1. **Fetch Settlement Reports** - Fetch Settlement Reports

---

## Step 1: Fetch Settlement Reports

### Endpoint

```
GET partners/{{partnerId}}/merchants/{{merchantId}}/reports
```

### Description

Fetch Settlement Reports

### Specifications

### Request Payload

```json
{}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request, indicated as either 'SUCCESS' or 'ERROR'. |
| `data` | array | Response data |
| `data.payoutId` | String | The payout ID used to identify a specific payout. |
| `data.merchantId` | String | The merchant ID of the merchant for which the settlement report is generated. |
| `data.transactionStartDate` | String | The date of the first transaction in the generated settlement report, in YYYY-MM-DD format. |
| `data.transactionEndDate` | String | The date of the last transaction in the generated settlement report, in YYYY-MM-DD format. |
| `data.settlementDate` | String | Date of the payout, in YYYY-MM-DD format. |
| `data.reportType` | String | Specifies the type of settlement report opted for the merchant. It can be either 'MONTHLY' or 'DAILY’. |
| `data.url` | String | URL from which the particular settlement report can be viewed. |
| `data.totalSale` | Number | The total amount of sales generated by the merchant during the report period. |
| `data.totalRefund` | Number | The total amount refunded by the merchant during the specified time period. |
| `data.fee` | Number | The total fee collected from transactions during the settlement. |
| `data.payout` | Number | The final amount paid out to the merchant after all deductions. |
| `message` | string | A message that describes the status of the request. |

### Guide

# Fetch Settlement Reports

Reporting APIs provide merchants with accurate business insights to track revenue, monitor transaction volumes, and reconcile financial records. Its retrieves a summary of a merchant’s settled transactions for a selected time period, such as daily or monthly, based on the configured report type.


---

## Environment Variables

The following template variables are used in this scenario:

| Variable | Description |
|----------|-------------|
| `{{partnerId}}` | Your partner ID |
| `{{merchantId}}` | The merchant ID |

## Quick Reference

| Step | Method | Endpoint | Purpose |
|------|--------|----------|---------|
| 1 | GET | `partners/{{partnerId}}/merchants/{{merchantId}}/reports` | Fetch Settlement Reports |

