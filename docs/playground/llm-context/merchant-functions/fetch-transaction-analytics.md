<!-- 
  Generated by: Surfboard Payments LLM Context Generator
  Version: 1.0.0
  Generated at: 2025-12-20T10:26:55.166Z
  
  This file is auto-generated. Do not edit manually.
-->

# API Integration Context: Fetch Transaction Analytics

> This document provides comprehensive context for integrating with our API.
> Use this with an LLM to get help implementing the Fetch Transaction Analytics flow.

## Overview

**Category:** Merchant Functions
**Scenario:** Fetch Transaction Analytics
**Description:** Fetch transaction analytics

Learn how to use other functionality of merchant

---

## Steps Overview

This scenario involves **1** step:

1. **Fetch Transaction Analytics** - To fetch the transaction analytics for the merchant

---

## Step 1: Fetch Transaction Analytics

### Endpoint

```
GET merchants/{{merchantId}}/analytics
```

### Description

To fetch the transaction analytics for the merchant

### Specifications

### Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `storeId` | string | No | Provide store ID to filter analytics for a specific store. |
| `terminalId` | string | No | Provide terminal ID to filter analytics for a specific terminal. |
| `startDate` | string | No | Start of the date range in yyyy-mm-dd format. |
| `endDate` | string | No | End of the date range in yyyy-mm-dd format. |
| `orderType` | string | No | Type of order to filter by. Possible values are 'purchase' and 'return'. |
| `terminalType` | string | No | Type of terminal to filter results. |
| `paymentMethod` | string | No | Payment method to filter analytics. |
| `posEntryMode` | string | No | POS entry mode to filter by. |
| `cardBrand` | string | No | Card brand to filter by. |
| `currency` | string | No | Three digit numeric or ISO code representing currency. |
| `groupBy` | object | No | Use this object to group analytics by store, terminal, etc. For example, groupBy[storeId]=true. |
| `groupBy.storeId` | boolean | No | Set to true to group results by store. |
| `groupBy.terminalId` | boolean | No | Set to true to group results by terminalId. |
| `groupBy.orderType` | boolean | No | Set to true to group results by orderType. |
| `groupBy.terminalType` | boolean | No | Set to true to group results by terminalType. |
| `groupBy.paymentMethod` | boolean | No | Set to true to group results by paymentMethod. |
| `groupBy.cardBrand` | boolean | No | Set to true to group results by cardBrand. |
| `groupBy.posEntryMode` | boolean | No | Set to true to group results by posEntryMode. |
| `groupBy.timeSegments` | string | No | Segments the analytics by time. Possible values are 'DAILY', 'WEEKLY', 'MONTHLY', 'QUARTERLY', 'YEARLY'.for example groupBy[timeSegments]=DAILY. |

### Request Payload

```json
{}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request, indicated as either 'SUCCESS' or 'ERROR'. |
| `data` | array | An array of analytics objects, each containing aggregated transaction information. |
| `data.currency` | string | The currency code for the transactions, typically a three-digit numeric code. |
| `data.terminalId` | string | The terminal ID from which the transactions were made. |
| `data.terminalType` | string | Specifies the type of terminal. |
| `data.orderType` | string | The type of order. |
| `data.paymentMethod` | string | The payment method used. |
| `data.cardBrand` | string | The brand of the card, if paymentMethod is 'CARD'. This may be empty if no card brand applies. |
| `data.timeSegments` | string | The date/time segment in which these transactions occurred. |
| `data.orderTransactionCount` | string | Number of transactions that match this grouping or filter. |
| `data.totalTransactionAmount` | number | Aggregate sum of transaction amounts (in minor currency units) for this group. |
| `data.message` | string | A message describing the status or outcome of the request. |
| `message` | string | A message describing the status or outcome of the request. |

### Guide

# Fetch Transaction Analytics

Retrieves aggregated transaction analytics for a specific merchant. This can include analytics by store, terminal, date range, card brand, and more.

{% callout type="note" label="Note" %}
To filter analytics by date range, send in startDate and endDate as query params. You can also use additional query parameters like storeId, terminalId, orderType, etc. Grouping the results can be controlled via the groupBy object in the query.
{% /callout %}

---

## Environment Variables

The following template variables are used in this scenario:

| Variable | Description |
|----------|-------------|
| `{{merchantId}}` | The merchant ID |

## Quick Reference

| Step | Method | Endpoint | Purpose |
|------|--------|----------|---------|
| 1 | GET | `merchants/{{merchantId}}/analytics` | To fetch the transaction analytics for the merchan... |

