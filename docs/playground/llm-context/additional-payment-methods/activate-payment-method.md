<!-- 
  Generated by: Surfboard Payments LLM Context Generator
  Version: 1.0.0
  Generated at: 2025-12-20T10:26:55.217Z
  
  This file is auto-generated. Do not edit manually.
-->

# API Integration Context: Activate Payment Method

> This document provides comprehensive context for integrating with our API.
> Use this with an LLM to get help implementing the Activate Payment Method flow.

## Overview

**Category:** Additional Payment Methods
**Scenario:** Activate Payment Method
**Description:** Activate a specific payment method for a merchant.

Learn how to use additional payment methods

---

## Steps Overview

This scenario involves **2** steps:

1. **Activate Payment Method** - Enable a specific payment method for a merchant.
2. **Fetch Payment Method Details** - Retrieve detailed information about a specific payment method.

---

## Step 1: Activate Payment Method

### Endpoint

```
POST merchants/{{merchantId}}/payment-methods
```

### Description

Enable a specific payment method for a merchant.

### Specifications

### Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `storeId` | string | No | Optional store identifier to associate payment methods with a specific store. |
| `merchantId` | string | No | Optional merchant identifier for the payment method configuration. |
| `card` | boolean | No | Card payment method configuration. Set to true to enable card payments. |
| `amex` | boolean | No | American Express payment method configuration. Set to true to enable AMEX payments. |
| `swish` | boolean | No | Swedish mobile payment method configuration. Set to true to enable Swish payments. |
| `klarna` | boolean | No | Buy now, pay later payment method configuration. Set to true to enable Klarna payments. |
| `b2binv` | boolean | No | B2B invoice payment method configuration. Set to true to enable B2B invoice payments. |
| `acc2acc` | boolean | No | Account to account transfer payment method configuration. Set to true to enable account to account transfers. |
| `svipps` | boolean | No | Norwegian mobile payment method. Set to true to enable Vipps payment method. |
| `smobilepay` | boolean | No | Danish mobile payment method. Set to true to enable MobilePay payment method. |

### Request Payload

```json
{
  "card": true,
  "swish": true,
  "b2binv": true,
  "acc2acc": true
}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request, indicated as either 'SUCCESS' or 'ERROR'. |
| `data` | array | Array of payment method registration results. |
| `data.method` | string | The payment method name (e.g., 'amex', 'swish', 'card'). |
| `data.paymentMethodId` | string | Unique identifier assigned to the payment method (present on success). |
| `data.status` | string | Registration status for this payment method, either 'SUCCESS' or 'ERROR'. |
| `data.message` | string | Error message if registration failed (present on error). |
| `message` | string | A message that describes the overall status of the request. |

### Guide

# Additional Payment Methods

Our in-store solution supports multiple payment methods, with card payments enabled by default for all merchants. You can use the Payment Methods API or the Partner Portal to add or remove other payment methods like AMEX and SWISH. These options can be set for specific merchants or stores to give you flexible control over how customers pay.

## Activate Payment Method

It is used to enable payment methods other than the default card option for a merchant. It allows you to activate multiple payment methods in a single request and apply specific configurations to each method as needed, giving you flexible control over the available payment options.

#### Using API

Create a `POST` request for enabling each payment method with a boolean value or configured with specific settings using an object.

#### Using Partner Portal

1. Log in to the **Partner Portal** → **Merchants** → select the desired merchant, then navigate to **Payment Methods**
2. In the page you can choose payment method as per your requirement and select the payment method.
3. Click **Activate Selected Methods** to apply the changes.

---

## Step 2: Fetch Payment Method Details

### Endpoint

```
GET merchants/{{merchantId}}/payment-methods/{{paymentMethodId}}
```

### Description

Retrieve detailed information about a specific payment method.

### Specifications

### Request Payload

```json
{}
```

### Response

### Response Parameters

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Status of the request, indicated as either 'SUCCESS' or 'ERROR'. |
| `data` | object | Response data |
| `data.paymentMethodId` | String | PaymentMethod ID of the payment method. |
| `data.paymentMethod` | String | Specifies the payment method. It can be one of the following: 'AMEX', 'SWISH', or 'CARD’. |
| `data.status` | String | Status of the payment method, indicated as either ACTIVATED' or 'DEACTIVATED’. |
| `data.amexMID` | Number | Returns AMEX MID for AMEX payment method. |
| `data.acquirerMID` | String | Returns the respective Acquirer MID for CARD payment method. |
| `message` | string | A message that describes the status of the request. |

### Guide

# Fetch Payment Method Details

It is used to retrieve the details of a specific payment method for a merchant. It allows you to view the configuration and status of a payment method, including its name, description, and any additional settings.


---

## Environment Variables

The following template variables are used in this scenario:

| Variable | Description |
|----------|-------------|
| `{{merchantId}}` | The merchant ID |
| `{{paymentMethodId}}` | The paymentMethodId value |

## Quick Reference

| Step | Method | Endpoint | Purpose |
|------|--------|----------|---------|
| 1 | POST | `merchants/{{merchantId}}/payment-methods` | Enable a specific payment method for a merchant. |
| 2 | GET | `merchants/{{merchantId}}/payment-methods/{{paymentMethodId}}` | Retrieve detailed information about a specific pay... |

